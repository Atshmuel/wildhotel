/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['encrypted-tbn0.gstatic.com', 'cdn.tatlerasia.com', 'lh3.googleusercontent.com', "i.ibb.co"]
    },
    // output: "export"
};

export default nextConfig;
